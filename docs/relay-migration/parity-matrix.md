# Relay migration parity matrix

Status values: `baseline`, `in progress`, `blocked on evidence`, `blocked on assets`, `ready for soak`.

| Surface | Current source/owner | Relay target | Status |
|---|---|---|---|
| Dashboard claim summary | Provider-neutral local game-data route publishes the Relay HTTP claim generation immediately; page navigation re-reads current state and preserves last-good stale data during an outage. A 2026-08-01 isolated worker run with a fresh SQLite database, scheduled jobs disabled, and no browser published a fresh joined region-19 claim generation within the bounded startup window | Relay HTTP claim snapshot | ready for soak |
| Shared refresh/error chrome | Provider-neutral live game-data status, stale-data warning, and automatic-recovery copy; never attributes a mixed migration page to BitJita | Current provider generation plus persisted last-good state | ready for soak |
| Browser current-data loader | `AppShell` uses provider-neutral `useGameData`; domain pages paint the last rendered snapshot immediately and still re-read the current local generation on every navigation. Focused-route pages such as Market, Planning, Craft Calculator, Sync, and Admin make no unused central request. The legacy endpoint map, 20-second short-circuit, BitJita browser proxy, and page helper routes are removed | Provider-neutral local domains plus focused local routes | ready for soak |
| Members roster | Provider-neutral local game-data route publishes the Relay HTTP member generation immediately; dependent regional player/equipment data follows independently without blocking the roster. The 2026-08-01 isolated no-browser worker proof published a fresh joined region-19 member generation from a fresh database, while route and browser tests prove last-good stale behavior and `503` only before any requested domain has loaded | Relay HTTP members snapshot | ready for soak |
| Member activity, skills, professions, Leaderboard, and quests | Professions and Leaderboard consume normalized Relay citizen/player domains with no provider-specific browser helper or copy; quests join member-filtered `traveler_task_state` with typed descriptions in the continuously connected primary-region session | Relay HTTP citizen levels plus member-filtered regional player/task subscription | ready for soak |
| Member equipment, buffs, Toolbelt, and housing | Members reads the live regional equipment/buff/preset domain; selected-member Toolbelt and housing use monitored-member-only Relay HTTP lookups with 15-second memory last-good caching and no SQL table | Proven member-filtered regional subscription, global equipment/tool/buff descriptions, and bounded Relay player inventory/housing | ready for soak |
| Inventory | Provider-neutral Inventory composes Relay HTTP shared storage with the primary-region session's two-stage `bank_state.claim_entity_id -> inventory_state.owner_entity_id` generation. Personal Town Bank rows retain exact player ownership and Item/Cargo identity, enrich from the local catalog, invalidate open pages immediately, and remain independently stale/partial during an outage. Selected-player bank categories use the bounded player-data service. No inventory-specific SQL table or scheduler exists. The 2026-08-01 live proof loaded 25 personal bank inventories and 863 occupied stacks with zero warnings | Relay HTTP joined inventory plus bounded regional Town Bank ownership joins | ready for soak |
| Active/passive crafts | Provider-neutral current page; Relay incomplete/completed claim rows split by typed recipe metadata | Relay HTTP plus global recipe catalog | ready for soak |
| Craft contributions | The primary regional typed session follows only current monitored-claim craft IDs. Positive `progressive_action_state` transaction deltas are attributed to the current row owner and appended immediately with exact TEXT amounts and durable transaction/craft deduplication. Initial subscription rows and zero/negative deltas do not create contributions. The scheduled evidence reconciler and its settings, cache, telemetry, and external API transport are removed | Regional progressive-action transactions plus committed Relay craft/member/catalog metadata and durable local history | ready for soak |
| Construction | Provider-neutral page and all remaining server compositions read the claim-filtered regional generation plus global catalogs; the legacy BitJita construction writer and collector control are retired | Regional state plus global catalogs | ready for soak |
| Research | Provider-neutral page and Craft Planner tier presets over claim-filtered regional technology state and the global technology catalog; scheduled collector retired | Regional tech state plus global catalogs | ready for soak |
| Recruitment | Members reads claim-filtered live regional posting state enriched from the global skill catalog; legacy endpoint and collector ownership retired | Regional recruitment state plus global skill catalog | ready for soak |
| Storage activity | Relay live HTTP tail on the 15-second provider loop, with bounded container rotation, exact item/cargo catalog enrichment, and idempotent durable copy into `activity_events`; legacy scheduled collector retired | Relay storage-log durable copy | ready for soak |
| Settlement market | Local Market and Dashboard current-listing metrics read the generic `market` generation from a continuously connected claim-scoped regional session. Orders and closed listings join exact owner usernames and typed item/cargo identities. Unique Hex Coin proceeds confirm sales; exact returned stacks confirm non-sales; ambiguous closures remain unresolved. History writes immediately after the current generation and uses exact TEXT amounts | Regional buy/sell/marketplace/closed-listing state plus local catalog projection and durable observed history | ready for soak |
| Global market tools | Market Browse catalog search and complete buy/sell order books, Overview current liquidity/hubs/open-order activity, Deals arbitrage, Barter Stalls, and Deal Watch now read the live normalized catalog plus bounded typed regional sessions through provider-neutral local routes. Deal Watch evaluates every committed `regional-market` generation immediately against a clearly labelled current sell-order median; its 30-minute job is reconciliation only, and its legacy price-history/claim-list crawl is removed. Discord `/price` and autocomplete use the same committed generation and catalog, reporting exact current sell/buy statistics and freshness without presenting open orders as completed sales. The orphaned browser Price Finder is removed because Market Browse is its complete live replacement. User watch configuration and alert/delivery history remain durable, but no Deal Watch or price-lookup current-state table exists. Exact decimal values, item/cargo identity, configured-region scope, generation invalidation, and last-good behavior are preserved. Completed trades, movers, volume, player-trade paths, and cross-market distance remain explicitly unavailable until their authoritative signals are proven. | Local catalog/order aggregation, authoritative observed history, bounded location joins, and adaptive region pool | in progress |
| Market sale notifications | Claim-scoped Relay closed-listing proceeds uniquely correlated to same-owner sell-order transitions; idempotent activity/outbox writes run immediately after generation publication | Relay closed-listing evidence plus durable outbox | ready for soak |
| Region | Active-region controls use continuous typed global population/control/name rows. Regional rankings use a continuously connected, region-scoped typed session over claim/local/tier rows and indexed point subscriptions for the 121 observed owner names; Relay measurements rejected OR-combined owner predicates because they remained unapplied. The Region page reads the generic `region` and `region-claims` generations through `/api/local/game-data`; manual refresh wakes the regional runtime, topology is rediscovered every 60 seconds, schema/source changes replace the session, malformed required rows preserve last-good data, and outage cause is persisted into freshness/subscription health. The retired five-minute endpoint, scheduled collector, legacy payloads, and unsupported trade-volume cards are gone. The 2026-08-01 live reconnect drill published 1,115 claims before and after one simulated failure through exactly two real sessions, with one recorded reconnect and no scope leakage | Global region metadata plus configured regional sessions | ready for soak |
| Adaptive region-session pool | Typed pool foundation pins the monitored region, single-flights concurrent opens, enforces configured-region and hard connection limits including in-flight opens, staggers warmups, and closes idle non-primary sessions. Public Craft, regional-market, and Empire runtimes are live consumers. Regional market and Empire own 15-second rotation loops, reconnect with bounded jittered backoff, reconcile settings without restart, and report freshness from each required region's own receive age | Shared runtime owner for cross-region crafts, market, region, map, and empires | in progress |
| Empires/watchtowers/siege | Provider-neutral overview, details, claim-member, watchtower, and current-siege views now read an atomic `empires` generation from bounded typed regional sessions. Region geometry scopes settlement/node/chunk rows; the primary session owns the replicated Empire/member identity graph while secondary sessions omit those duplicate rows. Membership transitions are written immediately after a complete primary-region generation and the scheduled membership collector is retired. Removed configured regions are pruned before reconnect and rejected by local routes even if an older last-good generation mentioned them. Freshness is calculated from the selected plus primary source, and last-good rows remain visible with their precise regional error. The legacy six-hour Hexite crawl and its five current/work tables are retired; the exact Empire treasury is projected immediately as a known minimum, while missing player/claim inventory and Foundry joins remain explicit blockers rather than zeroes. Siege participant roles remain explicitly `unknown`. No feature-specific current-state SQL table exists. | Regional typed subscriptions plus locally observed membership/history events | in progress |
| Deposits | Empires Hexite Deposits tab reads the provider-neutral 15-second Relay snapshot with last-good fallback; `unknown` and overdue rows are never promoted to active; no deposit-specific table or scheduled job | Relay HTTP deposits with explicit state | ready for soak |
| Map | Resource Finder reads continuously subscribed Relay `resource_desc` and `enemy_desc` rows from the normalized global catalog, active-region controls read the live global `region` generation, claim focus reads the live claim domain, and player tracking reads live member/player identities. The fetched legacy claim-layout payload had no UI or server consumer and is intentionally retired rather than recreated. No feature cache table or scheduled Map ingestion remains | Global resource/enemy/region rows plus live claim/member/player domains | ready for soak |
| Catalogs/planner inputs | Typed global item/cargo, recipe, extraction, item-list, resource, building-type, skill, and equipment descriptions project atomically into the normalized catalog and planner effort indexes. Craft Calculator and Craft Plan target search use the live local catalog index, and recipe trees compose direct and item-list producer routes from the current generation. Craft Planner settlement members/inventories and active/passive crafts read committed Relay generations, selected-player inventories use the bounded 15-second Relay service with no dedicated SQL cache, and workstation progress and save-time building reconciliation use the claim-filtered committed construction generation. Unavailable building data leaves targets pending rather than inventing completion | Global and regional typed subscriptions plus normalized current domains | ready for soak |
| Item/cargo icons | BitJita image URLs | Digest-verified local assets; permission confirmation recorded | blocked on assets |
| Activity/history/charts | Activity member filters now receive the current Relay member generation through `AppShell` with no page-level provider request or roster cache table; retained event/chart rows still need every legacy collector reconnected | Local SQLite derived from normalized domain events | in progress |
| Discord outbox and delivery | `/supplies`, `/online`, and `/crafts` read claim-fenced committed Relay claim/member/player/craft generations and the local catalog. Missing, partial, or malformed generations report unavailable instead of zero/offline/empty. The durable local outbox remains delivery-gated by environment | Relay current generations plus the existing outbox, default record mode until approval | ready for soak |
| Admin health | Relay provider topology/freshness/generation and subscription diagnostics; retired evidence-request telemetry and acquisition controls are absent | Provider topology/freshness/generation and live subscription health | ready for soak |
| Sync and public tools | Craft Calculator and Sync start no unused settlement request; Craft Calculator reads the live local catalog. Public Craft Finder reads the generic `public-crafts` generation built from indexed public-marker joins plus bounded owner/claim enrichment and local catalog projection, with exact effort arithmetic, exact workstation map fallback, no browser upstream call, and no dedicated SQL table. Marker churn queues a coherent follow-up generation instead of tearing down an applying subscription. The 2026-08-01 production verifier published 560 usable region-19 crafts; absent optional owner labels remain explicit warnings | Provider-neutral domain repository | ready for soak |

## Scheduled/background inventory

The worker no longer owns periodic settlement snapshots or any other migrated
current-data acquisition. Claim, members, citizens, players, inventories,
crafts, market, and catalog skills publish from committed Relay generations.
It owns committed-generation side effects, history, Discord outbox, reports,
health evaluation, notification generation, and maintenance/report delivery.
Craft contributions and completed sales are both event-driven; no game-data
evidence reconciler or external legacy API transport remains.
Regional buy-order acquisition and catalog refresh orchestration are also
absent from the scheduled path.

The browser BitJita proxy, legacy page endpoint map, dashboard/player/passive
craft/production helper routes, and their in-process response caches are
removed. Discord operational commands and Craft Plan building reconciliation
read committed Relay generations immediately. No replacement current-state SQL
cache was introduced.

Storage activity is no longer a scheduled ingestion job. The Relay live loop
backfills each newly observed storage container from the retained upstream
window and then reads a small tail, rotating bounded batches without delaying
the current inventory snapshot. SQLite remains only as durable event history
because Relay expires storage logs after roughly 15-16 days.

Current-state ingestion must become subscription-driven or use a bounded
single-flight Relay HTTP refresh loop. Scheduled jobs may reconcile, repair,
retain, aggregate, report, or deliver, but no current-data page may wait for a
periodic ingestion job. Each vertical milestone must also classify and remove
legacy tables that exist only for BitJita bulk-fetch, pagination, rate-limit,
or refresh-run bookkeeping. See
[live-first-data-policy.md](./live-first-data-policy.md).
